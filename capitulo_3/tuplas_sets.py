# las tuplas son similares a las tuplas, pero son inmutables

# ejemplo de definicion
tup = 1, 2, 3
tup = (1,2,3)

# tup.index() -> devuelve el valor ubicado en el indice
# tup.count() -> devuelve el tamañó de la tupla

#  los sets son similares a la lista, pero no pueden agregarse elementos repetidos

a = {5,3,4}
b = set([3,2,6])

# add -> agrega elementos al set
# a.intersection(b) -> busca los elementos comunes en a y b
# a.difference(b) -> busca los elementos unicos de a con respecto a b
# a.union(b) -> une los dos sets sin duplicar elementos