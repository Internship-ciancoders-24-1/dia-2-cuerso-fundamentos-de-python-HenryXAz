a = [1, 2]
b = [2, 3]

# a + b -> crea una nueva lista con todos los elementos de ambas listas

# a * 2 -> permite repetir los elementos de una lista tantas veces se desee multiplicar

# a.append() -> se agrega elementos al final de la lista
# a.pop() -> se elimina el último elemento de la lista, puede recibir un índice
# a.sort() -> ordenar la lista original
# del a[index] -> elimina un elemento en funcion de su índice
# a.remove(element) -> busca el elemento que se pasa por parametro y si lo encuentra, lo elimina
# len(a) -> devuelve el tamáño de la lista
# sorted(list) -> crea una copia de la lista y la ordena